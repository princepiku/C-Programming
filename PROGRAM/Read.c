//WAP to read n number of array elements and display.
#include<stdio.h>
void main()
{
	int a[10], n, i;
	printf("Enter the number elements : ");
	scanf("%d", &n);
	for(i = 0; i < n; i++)
	{
		printf("%d\n", i);
	}
}
